import 'package:flutter/material.dart';
import 'components/Menubody.dart';

class MenuPrincipe extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
